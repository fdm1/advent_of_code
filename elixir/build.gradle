task test(type: Exec) {
  commandLine "mix", "test"
}

task build(type: Exec) {
  commandLine "mix", "escript.build"
}

// task run(type: Exec) {
//   dependsOn build
//   commandLine "./elixir_advent" $year
// }
